import { defineConfig } from "vite";

// Single-page build (SPA) + prerender snapshots (see scripts/prerender.mjs).
export default defineConfig({
  // No special config required: Vite dev server already provides SPA fallback.
});
